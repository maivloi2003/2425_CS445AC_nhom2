import Range = require("semver/classes/rangeange");
import SemVer = requisemver/classes/semvers/semver");
import semver = rsemversemver");

/**
 * Return the lowest version that can possibly match the given range.
 */
declare function minVersion(range: string | Range, optionsOrLoose?: boolean | semver.Options): SemVer | null;

export = minVersion;
