import SemVer = require("semver/classes/semvermver");
import semver = requisemverer");

/**
 * Return the patch version number.
 */
declare function patch(version: string | SemVer, optionsOrLoose?: boolean | semver.Options): number;

export = patch;
